# Backend environment configuration
# Request the actual values from the user/environment manager. Do not commit real secrets.

# Database connection URL (default to local SQLite file)
BACKEND_DB_URL=sqlite:///./pricesense.db
